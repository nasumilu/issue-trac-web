import { HoverInteractionDirective } from './hover-interaction.directive';

describe('HoverSelectDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverInteractionDirective();
    expect(directive).toBeTruthy();
  });
});
