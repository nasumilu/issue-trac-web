import { IssueTracDirective } from './issue-trac.directive';

describe('IssueTracDirective', () => {
  it('should create an instance', () => {
    //const directive = new IssueTracDirective();
    // expect(directive).toBeTruthy();
  });
});
